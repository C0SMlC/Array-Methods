"use strict";

const array = [100, 80, 150, 10, 25, 50, 75, 200];
console.log(array.sort((a, b) =>a-b));

// const random = function (min, max) {
//   return math.random() * min + min;
// };

console.log(Math.trunc(0.5));